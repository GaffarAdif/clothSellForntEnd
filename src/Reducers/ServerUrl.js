const Server = 'http://localhost:3000/';

const SerVerUrlSave = (state = Server, action) => {
  // Return the state unchanged since you don't want to update it
  return state;
};

export default SerVerUrlSave;
